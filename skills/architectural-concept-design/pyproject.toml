[project]
name = "architectural-concept-design-runtime"
version = "0.2.0"
description = "Pinned deterministic runtime for the Architecture Pre-design Skill."
requires-python = ">=3.13,<3.14"
dependencies = [
  "jsonschema==4.23.0",
  "referencing==0.35.1",
]

[dependency-groups]
test = [
  "pytest==8.3.3",
  "PyYAML==6.0.2",
]

[tool.uv]
package = false
