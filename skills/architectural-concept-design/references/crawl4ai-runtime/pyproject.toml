[project]
name = "architecture-pre-design-crawl4ai-runtime"
version = "0.1.0"
description = "Independently locked Crawl4AI runtime for the Architecture Pre-design Skill."
requires-python = "==3.13.*"
dependencies = [
  "crawl4ai==0.9.2",
  "playwright==1.61.0",
]

[tool.uv]
package = false
